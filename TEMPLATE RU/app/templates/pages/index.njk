{%- import "./components/macro.njk" as macro with context -%}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/style.min.css">
    <title>{{ modelName }}</title>
  </head>
  <body>
    <div class="main-content-container webobscure">
      <div id="content-layout">
        
        {%- include "./blocks/header.njk" -%}

        <section class="full-img">
          {{ macro.image(modelName + '-block1-desc.webp', modelName + '-block1-mob.webp', "ONKRON", 'full-img__img') }}
        </section>


        <section class="full-img">
          {{ macro.image(modelName + '-block2-desc.webp', modelName + '-block2-mob.webp', "ONKRON", 'full-img__img') }}
        </section>
        
        <section class="full-img">
          {{ macro.image(modelName + '-block3-desc.webp', modelName + '-block3-mob.webp', "Продуманная конструкция", 'full-img__img') }}
        </section>
    <section class="full-img">
          {{ macro.image(modelName + '-block4-desc.webp', modelName + '-block4-mob.webp', "Подстраивается под вас", 'full-img__img') }}
        </section>
            <section class="full-img">
          {{ macro.image(modelName + '-block5-desc.webp', modelName + '-block5-mob.webp', "ONKRON", 'full-img__img') }}
        </section>
            <section class="full-img">
          {{ macro.image(modelName + '-block6-desc.webp', modelName + '-block6-mob.webp', "Кронштейн мечты в паре кликов от вас!", 'full-img__img') }}
        </section>
        {#- 
         
        <section class="full-img">
          {{ macro.image(modelName + '-block7-desc.webp', modelName + '-block7-mob.webp', "Кронштейн мечты в паре кликов от вас!", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block8-desc.webp', modelName + '-block8-mob.webp', "Меняйте высоту простым нажатием", 'full-img__img') }}
        </section>
       
        <section class="full-img">
          {{ macro.image(modelName + '-block9-desc.webp', modelName + '-block9-mob.webp', "Линейка моторизованных столов и подстольев ONKRON", 'full-img__img') }}
        </section>
        
        <section class="full-img">
          {{ macro.image(modelName + '-block10-desc.webp', modelName + '-block10-mob.webp', "Линейка моторизованных столов и подстольев ONKRON", 'full-img__img') }}
        </section>
        -#}
      </div>
    </div>
  </body>
</html>