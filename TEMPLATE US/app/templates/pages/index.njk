{%- import "./components/macro.njk" as macro with context -%}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/style.min.css">
    <title>{{ modelName }}</title>
  </head>
  <body>
    <div class="main-content-container webobscure">
      <div id="content-layout">
        {%- include "./blocks/header.njk" -%}
        <section class="full-img">
          {{ macro.image(modelName + '-block1-desc.jpg', modelName + '-block1-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        {#- {% include "./blocks/grid-layout.njk" %} -#}
        <section class="full-img">
          {{ macro.image(modelName + '-block2-desc.jpg', modelName + '-block2-mob.jpg', "Easy installation 3 steps", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block3-desc.jpg', modelName + '-block3-mob.jpg', "Maximum stability", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block4-desc.jpg', modelName + '-block4-mob.jpg', "Perfectly level installation", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block5-desc.jpg', modelName + '-block5-mob.jpg', "Perfect for home ", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block6-desc.jpg', modelName + '-block6-mob.jpg', "Excellent solution for business", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block7-desc.jpg', modelName + '-block7-mob.jpg', "Safe for active areas", 'full-img__img') }}
        </section>
        {#-
        <section class="full-img">
          {{ macro.image(modelName + '-block8-desc.jpg', modelName + '-block8-mob.jpg', "Stylish
classic", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block9-desc.jpg', modelName + '-block9-mob.jpg', "The VESA panel of the bracket is packaged at a slight angle.
You can easily adjust it yourself when installing the monitor.", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block10-desc.jpg', modelName + '-block10-mob.jpg', "ADV-1881 Dual screen  VESA panel", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block11-desc.jpg', modelName + '-block11-mob.jpg', "ATL-1881 Tilt adapter for  0° ~ +10° adjustment", 'full-img__img') }}
        </section>
                
                        -#}
      </div>
    </div>
  </body>
</html>