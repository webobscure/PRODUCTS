{%- import "./components/macro.njk" as macro with context -%}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/style.min.css">
    <title>{{ modelName }}</title>
  </head>
  <body>
    <div class="main-content-container webobscure">
      <div id="content-layout">
        {%- include "./blocks/header.njk" -%}
        <section class="full-img">
          {{ macro.image(modelName + '-block1-desc.jpg', modelName + '-block1-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        {#- {% include "./blocks/grid-layout.njk" %} -#}
        <section class="full-img">
          {{ macro.image(modelName + '-block2-desc.jpg', modelName + '-block2-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block3-desc.jpg', modelName + '-block3-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block4-desc.jpg', modelName + '-block4-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block5-desc.jpg', modelName + '-block5-mob.jpg', "Storage space out of light ", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block6-desc.jpg', modelName + '-block6-mob.jpg', "The stand of your dreams  is just a few clicks away!", 'full-img__img') }}
        </section>
        {#-

        <section class="full-img">
          {{ macro.image(modelName + '-block7-desc.jpg', modelName + '-block7-mob.jpg', "Built-in cable management system", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block8-desc.jpg', modelName + '-block8-mob.jpg', "Lower the TV stand to easily pass through the doorway", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block9-desc.jpg', modelName + '-block9-mob.jpg', "AWL-75 Set of lockable wheels", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block10-desc.jpg', modelName + '-block10-mob.jpg', "ADV-1881 Dual screen  VESA panel", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block11-desc.jpg', modelName + '-block11-mob.jpg', "ATL-1881 Tilt adapter for  0° ~ +10° adjustment", 'full-img__img') }}
        </section>
                
                        -#}
      </div>
    </div>
  </body>
</html>