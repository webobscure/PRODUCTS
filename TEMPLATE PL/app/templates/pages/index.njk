{%- import "./components/macro.njk" as macro with context -%}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/style.min.css">
    <title>{{ modelName }}</title>
  </head>
  <body>
    <div class="main-content-container webobscure">
      <div id="content-layout">
        
        {%- include "./blocks/header.njk" -%} 

        <section class="full-img">
          {{ macro.image(modelName + '-block1-desc.jpg', modelName + '-block1-mob.jpg', "ONKRON", 'full-img__img') }}
        </section>
        <section class="full-img">
          {{ macro.image(modelName + '-block2-desc.jpg', modelName + '-block2-mob.jpg', "Łatwy montaż", 'full-img__img') }}
        </section>

         {#- {% include "./blocks/grid-layout.njk" %} -#}

        <section class="full-img">
          {{ macro.image(modelName + '-block3-desc.jpg', modelName + '-block3-mob.jpg', "Maksymalna stabilność", 'full-img__img') }}
        </section>
          
        <section class="full-img">
          {{ macro.image(modelName + '-block4-desc.jpg', modelName + '-block4-mob.jpg', "Idealne wypoziomowanie", 'full-img__img') }}
        </section>
        
        <section class="full-img">
          {{ macro.image(modelName + '-block5-desc.jpg', modelName + '-block5-mob.jpg', "Idealny do domu", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block6-desc.jpg', modelName + '-block6-mob.jpg', "Świetne rozwiązanie dla f irm", 'full-img__img') }}
        </section>
        
      <section class="full-img">
          {{ macro.image(modelName + '-block7-desc.jpg', modelName + '-block7-mob.jpg', "Bezpieczny w strefach aktywności", 'full-img__img') }}
        </section>
    {#-
    <section class="full-img">
          {{ macro.image(modelName + '-block8-desc.jpg', modelName + '-block8-mob.jpg', "Stylowa
klasyka", 'full-img__img') }}
        </section>

        <section class="full-img">
          {{ macro.image(modelName + '-block9-desc.jpg', modelName + '-block9-mob.jpg', "Panel VESA stojaka jest spakowany pod lekkim kątem.
Możesz łatwo dostosować 
go samodzielnie podczas 
instalacji monitora.", 'full-img__img') }}
        </section>
        
         <section class="full-img">
          {{ macro.image(modelName + '-block10-desc.jpg', modelName + '-block10-mob.jpg', "ADV-1881 Podwójny panel VESA
na dwa ekrany", 'full-img__img') }}
        </section>

         <section class="full-img">
          {{ macro.image(modelName + '-block11-desc.jpg', modelName + '-block11-mob.jpg', "ATL-1881 Adapter do regulacji
nachylenia od 0° do +10°", 'full-img__img') }}
        </section>



        -#}
      </div>
    </div>
  </body>
</html>